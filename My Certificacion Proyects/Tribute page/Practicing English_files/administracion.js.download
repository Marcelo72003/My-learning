/*
* Cambia el archivo CSS
*/
/*para que se mantenga el alto contraste*/
$(document).ready(function() {
if(localStorage.getItem('estilos')==null || localStorage.getItem('estilos')==''){
}else{
   document.getElementById('cssArchivo').href=localStorage.getItem('estilos');
}
 });  
function cambiarArchivoCss(archivo) {
document.getElementById('cssArchivo').href=archivo;
localStorage.estilos=archivo;
}
/*
* Funcion para cambiar el tamaño del texto
*/
function zoomText(Action,Element){
    var obj=document.getElementById(Element); 
    var max = 100
    var min = 90 

    if (obj.style.fontSize==""){ 
        obj.style.fontSize="87.6%"; 
    }
    actual=parseInt(obj.style.fontSize); 
    increment=10;

    if(Action=="restore" ){ 
        obj.style.fontSize="87.6%" 
    }
    if( Action=="increase" && ((actual+increment) <= max )){
        value=actual+increment;
        obj.style.fontSize=value+"%"
    }
    if( Action=="decrease" && ((actual+increment) >= min )){
        value=actual-increment;
        obj.style.fontSize=value+"%"
    }
} 
/*Codigo para imprimir sólo una parte*/
function imprSelec(muestra)
{var ficha=document.getElementById(muestra);var ventimp=window.open(' ','popimpr');ventimp.document.write(ficha.innerHTML);ventimp.document.close();ventimp.print();ventimp.close();};

/*Funcion General al cargar el script
*
*script donde se le da la indicacion a los botones que carguen paginas o html dentro 
*del div contenedor de información
*/

///////
var intervaloActual = null;
var titulosRetroalimentacion = ["Atención","Resultado"];
//(function(){
    $(document).ready(function(e) {
		
    //Se agrega el evento onClick a cada div.
        
    $('#redcuaed').load('barra_acceso/barra.html') ;
    $('#redcuaed2').load('../barra_acceso/barra2.html') ;
    $('#menu_superior').load('menu_sup/menu.html') ;
    $('#menu_superior2').load('../menu_sup/menu2.html') ;
	
    var seccion_load = "";
    var params = getParams(); // obtenemos todos los parametros enviados a esta pagina
    var errorCode= unescape(params["errorcode"]); // obtenemos variable
    seccion_load = unescape(params["seccioncargar"]); // obtenemos variable
    var eventoCurso= unescape(params["evento"]); // obtenemos variable
    var valorCurso= unescape(params["valor"]); // obtenemos variable
    verificaError(errorCode); 
    
  //carga de contenido al iniciar
  //if((seccion_load === undefined) || seccion_load != ""){
  if(seccion_load != null && seccion_load != 'undefined' &&  seccion_load != ''){
       //cargamos estos datos solo si existe la variable y la pagina ha sido desplegada totalmente
      //Trabaja directamente con el aviso de privasidad
      verificaLoad(seccion_load);//cuando se necesita cargar una seccion especifica
      //alert(" entrar a la seccion "+seccion_load);
      console.log(seccion_load);
  }else{

  }
  
	
 // cambiarDestinoHref(elementoId, destino);

	//$('#cajadeacceso').load("login01.html"); // comentar
  

  //otros 
    aplicarEvento(eventoCurso, valorCurso); // mostrar ventana de logeo con curso especifico
	});    